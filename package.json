{
  "name": "openbook",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --build packages/contracts/tsconfig.json",
    "test": "npm run test --workspace @openbook/contracts"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
