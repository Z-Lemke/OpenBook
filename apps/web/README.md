# OpenBook web application

This workspace reserves the local Next.js learner application boundary. I01 deliberately
does not install a runtime, add routes, or add rendering; I03 will select the supported
Next/React version and consume `@openbook/contracts` here.
